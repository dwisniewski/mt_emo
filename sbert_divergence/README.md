These files provide comparisons between basic and emotional prompts. 
For each backtranslated text, we compare the text generated using a basic promtp (hyp1) with emotional prompt(hyp2), the index field indicates the ID of the text in the testset.
Then, SBERT-based similarity is calculated and the whole list is sorted by similarity ascending so that the top of the rankings are the most divergent examples.
